@model IEnumerable<VideoRentalSite.Models.videolist>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.videolist_quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.order.order_status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.video.video_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.videolist_quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.order.order_status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.video.video_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.videolist_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.videolist_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.videolist_id })
        </td>
    </tr>
}

</table>
